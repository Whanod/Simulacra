# Corpus manifest for slot 162000001.
# Hand-filled for the Whirlpool + DLMM composition test.
# This fixture is the SHARED slot that exercises the two-protocol
# composition path: a single ForkSpec that pulls both Whirlpool and DLMM
# state at the same slot must produce an InitialState whose fragments cover
# both protocols. The fixture committed alongside this manifest is
# SYNTHETIC (no live archival pull) — it composes the layout-faithful
# Whirlpool pool fixture from 160000001 and the layout-faithful DLMM pair
# fixture from 161000001 under one slot. Once 2.4 calibration pulls real
# archival data, both program_accounts files and the expected sections
# below should be replaced with mainnet-derived values without changing
# the test shape.
#
# The reserve-proxy / active-id schema mirrors the per-protocol manifests:
#   Whirlpool pools: pool_reserves[pubkey] = [liquidity, sqrt_price_x64]
#                    pool_tick_current_index[pubkey] = i32 tick index
#   DLMM pools:      pool_reserves[pubkey] = [active_id, bin_step]
#                    pool_active_id[pubkey] = i32 active bin
# Both programs share the same `pool_reserves` / `pool_*` namespaces but
# the validator routes each pubkey to its protocol via the program_id of
# the account that produced the fragment, so the keys do not collide as
# long as the protocols use disjoint pubkey conventions (which the
# synthetic fixtures do — Whirl* vs Dlmm*).

slot: 162000001
category: synthetic
block_height: 0
parent_slot: 0
blockhash: ""
programs:
  - "whirLbMiicVdio4qvUfM5KAg6Ct8VwpYzGff3uctyCc"
  - "LBUZKhRxPF3XUpBCjp4YzTKgLccjZhTSDM9YuVaPwxo"
expected:
  tx_count: 0           # synthetic; CALIBRATE-2.4 fills with real value
  decoded_coverage: null
  pool_reserves:
    "WhirlSolUsdc1111111111111111111111111111111": [25000000000000000000, 92233720368547758080]
    "DlmmSolUsdc11111111111111111111111111111111": [8388, 25]
    "DlmmUsdcUsdt1111111111111111111111111111111": [0, 1]
  pool_tick_current_index:
    "WhirlSolUsdc1111111111111111111111111111111": -42
  pool_active_id:
    "DlmmSolUsdc11111111111111111111111111111111": 8388
    "DlmmUsdcUsdt1111111111111111111111111111111": 0
thresholds: {}
